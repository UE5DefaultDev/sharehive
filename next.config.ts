/**
 * This file configures Next.js.
 * It's used to customize various aspects of the Next.js application, such as build settings,
 * environment variables, and routing.
 */
// Import the NextConfig type for better type checking and autocompletion.
import type { NextConfig } from "next";

// Define the Next.js configuration object.
const nextConfig: NextConfig = {
  /* Add your Next.js configuration options here.
   * Examples include:
   * - `output: 'standalone'` for a self-contained build.
   * - `images: { domains: ['example.com'] }` for image optimization.
   * - `env: { CUSTOM_VAR: 'value' }` for custom environment variables.
   * - `compiler: { removeConsole: true }` to remove console logs in production.
   */
};

// Export the configuration object.
export default nextConfig;